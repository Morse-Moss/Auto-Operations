# 小红书自动化运营系统项目规则

## 项目目标

构建一个小红书运营自动化系统。第一阶段只做灰豚数据爆文样本采集：从灰豚热词搜索和热词详情页采集热词、笔记列表、笔记详情，并写入本地 SQLite，供后续清洗、分析、内容生成 agent 使用。

## 当前阶段边界

第一阶段只做：
- 灰豚热词搜索采集
- 灰豚热词详情页采集
- 灰豚笔记列表和笔记详情弹窗采集
- SQLite 本地存储
- 可重复运行、去重、错误记录

第一阶段不做：
- 不破解灰豚加密接口
- 不抓小红书原文正文
- 不自动发布小红书
- 不生成文案或素材
- 不做复杂 Web 后台
- 不并发操作同一个灰豚登录态页面

## 技术约定

- 默认使用 Node.js + TypeScript。
- 浏览器自动化优先连接用户已有 Edge/Chrome 登录态。
- 数据库使用本地 SQLite，默认文件放在 `data/xhs-ops.sqlite`。
- 灰豚采集以页面 DOM 为主，不依赖未公开加密接口。
- 所有采集任务必须写入 `collection_runs`，失败要记录阶段、对象和错误信息。

## 目录约定

- `src/cli.ts`：命令行入口。
- `src/browser/`：灰豚页面操作和 DOM 解析。
- `src/db/`：SQLite schema、连接和写入逻辑。
- `src/types.ts`：跨模块共享类型。
- `tests/`：单元测试和解析测试。
- `data/`：本地数据库和运行产物，不提交敏感数据。
- `docs/superpowers/specs/`：设计规格文档。
- `docs/superpowers/plans/`：实施计划。

## 采集安全规则

- 不高频打开大量灰豚页面；默认串行采集。
- 不绕过灰豚权限、登录或风控限制。
- 不保存账号密码、cookie、token 到代码或文档。
- 页面结构变化时先记录失败快照，再调整解析器。

## 开发规则

- 非 trivial 修改必须先有设计或计划。
- 先写可验证的解析/数据库测试，再写实现。
- 每次实现后运行相关测试。
- 手术式修改，不顺手重构无关代码。
- 不自动 git push。当前目录不是 git 仓库时，不要求提交。
