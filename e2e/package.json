{
  "name": "spider-xhs-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test --browser=chromium --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0"
  }
}
